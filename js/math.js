//STEP 1: an application that prompt the user for anumber and then displays the absolute value of the number
//let absValue = window.prompt("Enter a number, positive or negative:");
//window.console.log(Math.abs(absValue));

//STEP 2: an application that prompts the user for a decimal number, rounds the number up to the nearest whole number, and then displays the result
//let decimalVal = window.prompt("Enter a floating point (decimal) number:");
//window.console.log(Math.ceil(decimalVal));

//STEP 3: an application that prompts the user for a decimal number, stores the result in a variable, and then rounds down to the nearest whole number and displays the result
//let decimalVal = window.prompt("Enter a floating point (decimal) number:");
//window.console.log(Math.floor(decimalVal));

//STEP 4: an application that prompts the user for 5 comma delimited numbers, stores the numbers, and then finds and displays both in the console
//let numString = window.prompt("Enter five numbers, each separated by a comma:");
//let numArray = numString.split(',');
//for (let i = 0; i < numArray.length; i++) {
//   numArray[i] = parseInt(numArray[i]);
//}
//window.console.log(`The lowest number entered was: ${Math.min(...numArray)}`);
//window.console.log(`The highest number entered was: ${Math.max(...numArray)}`);

//STEP 5: an application that prompts the user for a number and calculates and displays the square root of the number
//let numRoot = window.prompt("Enter a number, and I will tell you the square root:");
//window.console.log(`The square root of ${numRoot} is ${Math.sqrt(numRoot)}`);




